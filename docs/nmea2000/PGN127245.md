# PGN127245 API

Class `nmea2000.PGN127245` (Python) / `nmealib::nmea2000::PGN127245` (C++).

## PGN Overview
CANboat: PGN 127245 - Rudder. Reports rudder instance, direction, ordered angle, and actual rudder position.

## Creation
- Python direct:
```python
msg = nmealib.nmea2000.PGN127245(<fields...>)
```
- C++ direct:
```cpp
nmealib::nmea2000::PGN127245 msg(/* fields */);
```
- Python factory:
```python
msg = nmealib.nmea2000.Nmea2000Factory.create(raw_can_frame)
```
- C++ factory:
```cpp
auto msg = nmealib::nmea2000::Nmea2000Factory::create(rawCanFrame);
```

## Public Methods

### `get_angle_order` / `getAngleOrder`

```cpp
SignedAngle getAngleOrder() const noexcept;
```

Returns the angle order value parsed from this PGN.

### `get_angle_order_degrees` / `getAngleOrderDegrees`

```cpp
float getAngleOrderDegrees() const noexcept;
```

Returns the angle order degrees value parsed from this PGN.

### `get_direction` / `getDirection`

```cpp
HalfByte getDirection() const noexcept;
```

Returns the direction value parsed from this PGN.

### `get_direction_string` / `getDirectionString`

```cpp
std::string getDirectionString() const noexcept;
```

Returns the direction string value parsed from this PGN.

### `get_position` / `getPosition`

```cpp
SignedAngle getPosition() const noexcept;
```

Returns the position value parsed from this PGN.

### `get_position_degrees` / `getPositionDegrees`

```cpp
float getPositionDegrees() const noexcept;
```

Returns the position degrees value parsed from this PGN.

### `get_rudder_id` / `getRudderId`

```cpp
uint8_t getRudderId() const noexcept;
```

Returns the rudder id value parsed from this PGN.

### `get_string_content` / `getStringContent`

```cpp
std::string getStringContent(bool verbose) const noexcept override;
```

Returns a human-readable representation of the PGN content.

## Notes
- Inherits `Message2000` and `Message` APIs (`serialize`, `validate`, `clone`, CAN/PGN getters).
- PGN overview source: https://canboat.github.io/canboat/canboat.html
