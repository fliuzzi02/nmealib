# PGN130306 API

Class `nmea2000.PGN130306` (Python) / `nmealib::nmea2000::PGN130306` (C++).

## PGN Overview
CANboat: PGN 130306 - Wind Data. Reports wind speed, wind direction, and wind reference.

## Creation
- Python direct:
```python
msg = nmealib.nmea2000.PGN130306(<fields...>)
```
- C++ direct:
```cpp
nmealib::nmea2000::PGN130306 msg(/* fields */);
```
- Python factory:
```python
msg = nmealib.nmea2000.Nmea2000Factory.create(raw_can_frame)
```
- C++ factory:
```cpp
auto msg = nmealib::nmea2000::Nmea2000Factory::create(rawCanFrame);
```

## Public Methods

### `get_reserved1` / `getReserved1`

```cpp
HalfByte getReserved1() const noexcept;
```

Returns the reserved1 value parsed from this PGN.

### `get_reserved2` / `getReserved2`

```cpp
Byte getReserved2() const noexcept;
```

Returns the reserved2 value parsed from this PGN.

### `get_reserved3` / `getReserved3`

```cpp
Byte getReserved3() const noexcept;
```

Returns the reserved3 value parsed from this PGN.

### `get_sequence_id` / `getSequenceId`

```cpp
uint8_t getSequenceId() const noexcept;
```

Returns the sequence id value parsed from this PGN.

### `get_string_content` / `getStringContent`

```cpp
std::string getStringContent(bool verbose) const noexcept override;
```

Returns a human-readable representation of the PGN content.

### `get_wind_direction` / `getWindDirection`

```cpp
Angle getWindDirection() const noexcept;
```

Returns the wind direction value parsed from this PGN.

### `get_wind_direction_degrees` / `getWindDirectionDegrees`

```cpp
float getWindDirectionDegrees() const noexcept;
```

Returns the wind direction degrees value parsed from this PGN.

### `get_wind_reference` / `getWindReference`

```cpp
HalfByte getWindReference() const noexcept;
```

Returns the wind reference value parsed from this PGN.

### `get_wind_speed` / `getWindSpeed`

```cpp
Speed getWindSpeed() const noexcept;
```

Returns the wind speed value parsed from this PGN.

### `get_wind_speed_knots` / `getWindSpeedKnots`

```cpp
float getWindSpeedKnots() const noexcept;
```

Returns the wind speed knots value parsed from this PGN.

## Notes
- Inherits `Message2000` and `Message` APIs (`serialize`, `validate`, `clone`, CAN/PGN getters).
- PGN overview source: https://canboat.github.io/canboat/canboat.html
